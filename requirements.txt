# ── UI / 기본 ─────────────────────────────────────────────
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
joblib>=1.3.0
matplotlib>=3.8.0
altair>=5.0.0

# ── 문항 적합성(SBERT) · 질문 군집화 · 핵심 주장 명확성(KoSimCSE) ──
# 최초 실행 시 모델 자동 다운로드(인터넷 필요): ko-sroberta-multitask, KoSimCSE-roberta
sentence-transformers>=2.7.0
kss>=5.0.0

# ── 자기표현 차별성 (role04 dependency_parser) ─────────────
spacy>=3.8.0,<3.9.0
kiwipiepy>=0.20.0
# 한국어 spacy 모델 (pip로 함께 설치 → 수동 download 불필요)
ko_core_news_sm @ https://github.com/explosion/spacy-models/releases/download/ko_core_news_sm-3.8.0/ko_core_news_sm-3.8.0-py3-none-any.whl

# ── 표현 명료성 (role03 hedge, konlpy/Okt) ────────────────
# konlpy(Okt)는 Java JDK + JAVA_HOME 필요. 미설치 시 규칙 기반으로 자동 폴백(앱은 정상 동작).
konlpy>=0.6.0
JPype1>=1.5.0

# ── 데이터(xlsx) 읽기 ─────────────────────────────────────
openpyxl>=3.1.0
